apiVersion: v1
kind: Service
metadata:
  name: iris-api-service
  labels:
    app: iris-api
spec:
  type: NodePort
  selector:
    app: iris-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
